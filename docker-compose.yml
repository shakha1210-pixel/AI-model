services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./:/app
    restart: unless-stopped

  # TODO: agar ENABLE_DATABASE=true qilib PostgreSQL ishlatmoqchi bo'lsangiz,
  # quyidagi xizmatni yoqing va .env dagi DATABASE_URL ni shunga mos
  # o'zgartiring, masalan:
  #   DATABASE_URL=postgresql://agent:change_me@db:5432/agent_db
  #
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     POSTGRES_USER: agent
  #     POSTGRES_PASSWORD: change_me
  #     POSTGRES_DB: agent_db
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #   ports:
  #     - "5432:5432"

# volumes:
#   postgres_data:
